{
  "name": "personal_site",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build-client && node app",
    "start-local": "npm run build-client && PORT=8080 node app",
    "start-dev": "npm run watch && PORT=8080 nodemon app",
    "build-client": "cd client && npm run build && cd ..",
    "watch": "cd client && npm run watch && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  }
}
